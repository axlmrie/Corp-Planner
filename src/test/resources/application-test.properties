# ==========================================
# BASE DE DONNÉES H2 (En mémoire pour les tests)
# ==========================================
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# ==========================================
# SÉCURITÉ JWT (Clés bidons pour que ça démarre)
# ==========================================
application.security.jwt.secret-key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
application.security.jwt.expiration=86400000
application.cors.allowed-origins=*
# ==========================================
# MAILJET (Valeurs fictives pour éviter le crash du Bean EmailService)
# ==========================================
mailjet.api-key=fake-api-key
mailjet.secret-key=fake-secret-key
application.mail.from=test@test.com

# ==========================================
# DIVERS
# ==========================================
upload.directory=target/test-uploads
logging.level.org.springframework.web=ERROR
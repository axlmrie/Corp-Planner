# 1. Base de données de TEST (H2 en mémoire)
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# 2. Configuration CORS pour les tests
# On met "*" pour que les tests MockMvc ne soient pas bloqués
application.cors.allowed-origins=*

# 3. Sécurité et JWT pour les tests
application.security.jwt.secret-key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
application.security.jwt.expiration=3600000

# 4. Configuration Mail pour les tests (Désactivé)
spring.mail.host=localhost
spring.mail.port=2525
application.mail.from=test@calip.fr

upload.directory=C:/01-Reprise-compta/Assets
upload.url-prefix=/uploads/**